import { Routes } from '@angular/router';
import { FeedPageComponent } from './modules/feed/feed-page.component';

export const routes: Routes = [
  {
    path: '',
    component: FeedPageComponent,
  },
];
